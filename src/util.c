/*
 * Returns the lesser of the two given integers.
 */
int min(int a, int b) {
    return a < b ? a : b;
}

/*
 * Returns the greater of the two given integers.
 */
int max(int a, int b) {
    return a > b ? a : b;
}
